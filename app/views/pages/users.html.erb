<% @title="Site Users" %>

<table class="table table-striped table-responsive">
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>ID</th>
  </tr>
  <tbody>
  <% @users = User.all %>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.id %></td>
    </tr>
  <% end %>
  </tbody>
</table>

To modify from the console:
<pre>
morgan = User.find(10)
morgan.update_attribute(:name,'Morgan')
</pre>